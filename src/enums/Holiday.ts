export enum HolidayType {
  NATIONAL = 'nacional',
  BRIDGE = 'puente', // Feriados puente
  CUSTOM = 'personalizado', // Días custom de la empresa
}

export enum HolidaySource {
  API = 'api',
  MANUAL = 'manual',
}

export enum Country {
  ARGENTINA = 'AR',
  BRAZIL = 'BR',
  URUGUAY = 'UY',
}
